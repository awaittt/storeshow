import React, { FC } from "react";
import styles from "./index.module.scss";

interface IProps {}
const MapModal: FC<IProps> = ({}) => {
  return (
<div id="container" className={styles.main} >

</div> 
  )
};

export { MapModal };
